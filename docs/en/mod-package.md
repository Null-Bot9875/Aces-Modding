# MOD package structure

This page will define the author-facing rules for manifests, entry Lua files, configuration directories, localization, asset references, and package dependencies.

TODO: freeze directory conventions, required files, load order, name collisions, and error behavior from verified runtime evidence.
