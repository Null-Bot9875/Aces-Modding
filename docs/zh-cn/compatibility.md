# 游戏更新与 MOD 兼容性

制作 MOD 时，只依赖文档明确列出的配置字段和 Lua API。游戏内部名称即使能够访问，也可能在游戏更新后改变。

## 当前状态

首个支持的游戏版本和 MOD 版本写法正在准备中。在兼容性说明可用前，请把这里的模板和示例视为预览内容，不要用于长期维护的 MOD。

以后每次影响 MOD 的改动都会写进[更新记录](../../CHANGELOG.md)，并说明作者需要修改什么。
