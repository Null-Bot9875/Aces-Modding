# Integrated node-pack example

The finished example should demonstrate Card → Enemy action chain → Node → reward, giving authors a working base they can modify incrementally.

TODO: add the example after all three content contracts and real-game acceptance are complete. This directory is not currently a runnable MOD.
