# Game Configuration Reference

The current package contains the game configuration for Aces `1.0.8`:

- [aces-game-config-1.0.8.zip](aces-game-config-1.0.8.zip)

Use this archive to look up existing IDs and configuration structures. It is read-only reference material and cannot be installed by copying it into `Mods/`.
