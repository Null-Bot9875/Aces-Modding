# 游戏 Lua 文件

每次游戏版本发布时，本目录同时提供一份相同版本的 Lua 文件压缩包：

```text
aces-game-lua-<游戏版本>.zip
```

本目录不展开保存整棵源码目录，只放这一个压缩包。

游戏包中的 Lua 本身也是可读源码，只是以 `.lua.bytes` 文件放在游戏资源包中。这里的压缩包会恢复为普通 `.lua` 文件并保留原目录，省去你自行解包、提取和改名的步骤。它不是反编译或解密得到的文件。

这些文件用于查阅游戏现有写法。制作 MOD 时，只依赖[Lua API 文档](../docs/zh-cn/lua-api.md)明确列出的接口。
